

# 外部 SSO 登录集成 — AI 实现提示词

## 概述

你需要在现有系统中实现一个 **SSO 单点登录** 端点，让用户从问卷系统跳转到你这里完成身份认证，再携带签名票据跳回问卷系统。这是一个**标准的一次性票据签发流程**，不是 OAuth 2.0 也不是 OIDC。
参考mock_external.py 文件的实现
---

## 一、完整流程时序

```
 浏览器                    问卷系统                        外部系统（你的服务）
  │                          │                                  │
  │  ① 点击"外部登录"        │                                  │
  │ ───────────────────────→ │                                  │
  │                          │  ② 生成随机 state（32-200 字符） │
  │                          │  ③ 302 跳转到外部系统            │
  │ ←─────────────────────── │                                  │
  │  ④ 携带 ?state=xxx 访问  │                                  │
  │ ──────────────────────────────────────────────────────────→ │
  │                          │                                  │
  │                          │  ⑤ 展示登录页面（用户输入身份）   │
  │                          │  ⑥ POST /launch 提交             │
  │                          │                                  │
  │                          │  ⑦ 验证输入，用 HS256 签发 JWT    │
  │                          │  ⑧ 303 跳回问卷回调地址           │
  │ ←────────────────────────────────────────────────────────── │
  │                          │  ⑨ 问卷端：                         │
  │                          │     - 解析 JWT，验证签名            │
  │                          │     - 对比 state 与之前发出的是否一致 │
  │                          │     - 校验 iat/exp 时间窗           │
  │                          │     - 建立用户会话，登录成功         │
```

---

## 二、你需要实现的两个接口

### 2.1 `GET /` — 展示登录页面

| 项目 | 说明 |
|---|---|
| 查询参数 | `state`（必填，32-200 字符） |
| 无 state 时的行为 | 返回 **303** 跳转到问卷起始 URL（见配置项 `SURVEY_LOGIN_START_URL`） |
| 有 state 时的行为 | 渲染 HTML 登录表单 |

**为什么 state 必填且不能为空？** 它是 CSRF 防伪造的凭证。问卷系统生成一个随机字符串发给外部，外部在 JWT 中原封不动带回，问卷系统校验一致性来防止攻击者伪造回调请求。所有主流 SSO 协议（OAuth 2.0、OIDC、SAML）都有此机制。

**表单要求：**

| 字段 | name | 最大长度 | 说明 |
|---|---|---|---|
| 隐藏字段 | `state` | 200 | 从 URL 查询参数中传入，原样提交 |
| 用户 ID | `external_user_id` | 128 | 必填，用户的唯一标识 |
| 用户名 | `username` | 100 | 必填，展示用显示名称 |

表单提交地址为 `POST /launch`，编码方式为 `application/x-www-form-urlencoded`。

### 2.2 `POST /launch` — 处理登录并签发票据

| 项目 | 说明 |
|---|---|
| 接收参数 | `external_user_id`、`username`、`state`（均为表单字段） |
| 参数校验 | 非空、不超过各自最大长度、不含控制字符 |
| 成功 | 签发 JWT，**303** 重定向到问卷回调地址 |
| 参数无效 | 返回 **422**，说明原因 |

---

## 三、JWT 票据规范

### 签名算法

`HS256`（HMAC-SHA256）

### 签名密钥

从环境变量 `EXTERNAL_SSO_SHARED_SECRET` 读取，长度**至少 32 字符**。不要硬编码、不要使用示例值。

### 载荷（Payload）字段

```json
{
  "iss": "dml-demo-external",
  "aud": "dml-survey",
  "sub": "用户的 external_user_id",
  "username": "用户的 username（原样传递，不含控制字符）",
  "iat": 1711699200,
  "exp": 1711699260,
  "jti": "a1b2c3d4e5f6...（uuid4 hex）",
  "state": "从表单接收的 state 值，原封不动",
  "token_type": "external_sso"
}
```

| 字段 | 必填 | 说明 |
|---|---|---|
| `iss` | 是 | 签发者标识，固定为 `dml-demo-external` |
| `aud` | 是 | 接收方标识，固定为 `dml-survey` |
| `sub` | 是 | 用户 ID，最长 128 字符 |
| `username` | 是 | 显示名称，最长 100 字符 |
| `iat` | 是 | 签发时间（UTC Unix 时间戳） |
| `exp` | 是 | 过期时间 = iat + 60 秒（范围 30-300 秒） |
| `jti` | 是 | 唯一票据 ID，每次签发不同，用 uuid4().hex 生成 |
| `state` | 是 | 原样传递问卷发来的 state，不能修改、不能省略 |
| `token_type` | 是 | 固定为 `external_sso` |

### 回调重定向

将 JWT 作为 `ticket` 参数附加到 `SURVEY_CALLBACK_URL` 上：

```
SURVEY_CALLBACK_URL?ticket=eyJhbGciOiJIUzI1NiIs...
```

如果回调 URL 已有查询参数，用 `&` 连接；否则用 `?` 连接。

---

## 四、配置项

所有配置在部署时设置，不要在代码中硬编码实际值（提供默认占位即可）：

| 配置项 | 类型 | 说明 | 示例值 |
|---|---|---|---|
| `EXTERNAL_SSO_SHARED_SECRET` | string, >=32字符 | JWT 签名密钥（环境变量） | `dd547ede87770534fb70aa0d23ee2bef...` |
| `EXTERNAL_SSO_ISSUER` | string | JWT `iss` 字段值 | `dml-demo-external` |
| `EXTERNAL_SSO_AUDIENCE` | string | JWT `aud` 字段值 | `dml-survey` |
| `SURVEY_CALLBACK_URL` | string | 问卷接收票据的地址 | `http://10.17.158.73:8280/api/v1/auth/external/callback` |
| `SURVEY_LOGIN_START_URL` | string | 问卷发起 SSO 的入口 | `http://10.17.158.73:8280/api/v1/auth/external/start` |
| `EXTERNAL_TICKET_MAX_SECONDS` | int, 30-300 | JWT 有效期秒数 | `60` |

**启动时校验：** 检查所有配置项已填写、密钥 >= 32 字符且不是示例值、过期时间在范围内。缺失或无效则返回 **503** 并明确提示缺少哪些配置。

---

## 五、输入校验规则

| 字段 | 规则 |
|---|---|
| `external_user_id` | 1-128 字符，trim 前后空白，不含 Unicode 控制字符 |
| `username` | 1-100 字符，trim 前后空白，不含 Unicode 控制字符 |
| `state` | 32-200 字符，原样传递，不修改 |

控制字符检测：Unicode 类别以 `"C"` 开头的字符（Cc、Cf、Cs、Co、Cn）。

---

## 六、安全要点汇总

1. **state 不可为空**，必须原封不动写入 JWT 并传回问卷端做 CSRF 校验
2. **密钥不能硬编码**，从环境变量读取，至少 32 字符
3. **JWT 有效期短**（最多 300 秒，推荐 60 秒），降低票据泄露风险
4. **一次性票据**：每个 `jti` 只能使用一次（问卷端做去重校验）
5. **用户输入必须清洗**：trim + 控制字符过滤，防止注入
6. **不能跳过校验步骤**：即使开发调试期间，所有配置校验和参数校验都不应绕过

---

## 七、技术栈建议

- **语言/框架不限**，只要能签发 HS256 JWT 即可
- JWT 库：推荐使用各语言官方推荐的库（如 Python 的 `PyJWT`、Node.js 的 `jsonwebtoken`、Java 的 `jjwt` 等）
- 如果你的框架支持表单解析和 303 重定向（几乎所有 Web 框架都支持），不需要额外依赖
- 整个功能只需要 **2 个端点**（`GET /` 和 `POST /launch`），是一个轻量集成

---

## 八、参考交互示例

### 正常流程

```
① 浏览器请求外部系统（无 state）
   GET / →
   303 Location: http://10.17.158.73:8280/api/v1/auth/external/start

② 问卷返回 state 后跳回外部系统
   GET /?state=2f8a1b3c4d5e6f7g8h9i0j1k2l3m4n5o →
   200 → 渲染登录表单

③ 用户填写信息提交
   POST /launch
     external_user_id=demo-1001
     &username=张三
     &state=2f8a1b3c4d5e6f7g8h9i0j1k2l3m4n5o →
   303 Location: http://10.17.158.73:8280/api/v1/auth/external/callback?ticket=eyJ...
```

### 错误场景

| 场景 | 状态码 | 说明 |
|---|---|---|
| 配置缺失 | 503 | 启动时直接报错，不启动服务 |
| state 参数无效 | 422 | 太长、太短或包含非法字符 |
| 用户信息无效 | 422 | 空值、超长或含控制字符 |
